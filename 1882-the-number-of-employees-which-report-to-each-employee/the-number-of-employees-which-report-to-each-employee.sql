SELECT E.EMPLOYEE_ID, E.NAME, COUNT(R.REPORTS_TO) AS REPORTS_COUNT, ROUND(AVG(R.AGE)) AS AVERAGE_AGE FROM EMPLOYEES E JOIN EMPLOYEES R ON E.EMPLOYEE_ID = R.REPORTS_TO GROUP BY E.EMPLOYEE_ID, E.NAME HAVING REPORTS_COUNT > 0 ORDER BY E.EMPLOYEE_ID;